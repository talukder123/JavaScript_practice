/**
 * let name = "JavaScript";
Print the uppercase version.
Print the lowercase version.
Print the string with whitespace trimmed: "   hi there   " → "hi there"

 */

let name = "JavaScript";

const upp = name.toUpperCase();
console.log('Uppercase:', upp);

const loww = name.toLowerCase();
console.log('Lowercase:', loww);

// ------------------------------------
let trimmed = "   hi there   ";

const trim_ed = trimmed.trim();
console.log(trim_ed);