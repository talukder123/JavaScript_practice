/*
 * Convert the string "JavaScript" into an array of characters.
Convert the array ["c", "a", "t"] back into a single string "cat".
 */

const str = "JavaScript";

const temp = str.split("");
console.log(temp);

const cat = ['c', 'a', 't'];
const temp2 = cat.join("");

console.log(temp2);